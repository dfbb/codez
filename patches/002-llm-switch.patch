diff --git a/codex-rs/core/src/client.rs b/codex-rs/core/src/client.rs
index 73d7398a3..7757bdf1f 100644
--- a/codex-rs/core/src/client.rs
+++ b/codex-rs/core/src/client.rs
@@ -182,6 +182,7 @@ struct ModelClientState {
     attestation_provider: Option<Arc<dyn AttestationProvider>>,
     disable_websockets: AtomicBool,
     cached_websocket_session: StdMutex<WebsocketSession>,
+    model_provider_id: String,
 }
 
 /// Resolved API client setup for a single request attempt.
@@ -380,6 +381,7 @@ impl ModelClient {
         beta_features_header: Option<String>,
         item_ids_enabled: bool,
         attestation_provider: Option<Arc<dyn AttestationProvider>>,
+        model_provider_id: String,
     ) -> Self {
         let model_provider = create_model_provider(provider_info, auth_manager);
         let codex_api_key_env_enabled = model_provider
@@ -404,6 +406,7 @@ impl ModelClient {
                 attestation_provider,
                 disable_websockets: AtomicBool::new(false),
                 cached_websocket_session: StdMutex::new(WebsocketSession::default()),
+                model_provider_id,
             }),
             prompt_cache_key_override: None,
         }
@@ -1321,13 +1324,23 @@ impl ModelClientSession {
             let inference_trace_attempt = inference_trace.start_attempt();
             inference_trace_attempt.add_request_headers(&mut options.extra_headers);
             inference_trace_attempt.record_started(&request);
-            let client = ApiResponsesClient::new(
-                transport,
-                client_setup.api_provider,
-                client_setup.api_auth,
-            )
-            .with_telemetry(Some(request_telemetry), Some(sse_telemetry));
-            let stream_result = client.stream_request(request, options).await;
+            let stream_result = match codez_llm_switch::route(&self.client.state.model_provider_id) {
+                None => {
+                    // 原生路径:遥测链完整保留
+                    let client = ApiResponsesClient::new(
+                        transport,
+                        client_setup.api_provider,
+                        client_setup.api_auth,
+                    )
+                    .with_telemetry(Some(request_telemetry), Some(sse_telemetry));
+                    client.stream_request(request, options).await
+                }
+                Some(rt) => {
+                    // 接管路径:连接器自带 HTTP/SSE;api_auth 仅作 bearer 退路。
+                    // transport / api_provider / request_telemetry / sse_telemetry 在本臂未用,作用域结束自动 drop。
+                    codez_llm_switch::run(rt, request, client_setup.api_auth).await
+                }
+            };
 
             match stream_result {
                 Ok(stream) => {
diff --git a/codex-rs/core/src/client_tests.rs b/codex-rs/core/src/client_tests.rs
index 1d0c4c48c..e64588f6a 100644
--- a/codex-rs/core/src/client_tests.rs
+++ b/codex-rs/core/src/client_tests.rs
@@ -78,6 +78,7 @@ fn test_model_client(session_source: SessionSource) -> ModelClient {
         /*beta_features_header*/ None,
         /*item_ids_enabled*/ false,
         /*attestation_provider*/ None,
+        /*model_provider_id*/ String::new(),
     )
 }
 
@@ -571,6 +572,7 @@ fn model_client_with_counting_attestation(
         Some(Arc::new(CountingAttestationProvider {
             calls: attestation_calls.clone(),
         })),
+        /*model_provider_id*/ String::new(),
     );
     (model_client, attestation_calls)
 }
diff --git a/codex-rs/core/src/session/session.rs b/codex-rs/core/src/session/session.rs
index 0c07f5e95..313837acc 100644
--- a/codex-rs/core/src/session/session.rs
+++ b/codex-rs/core/src/session/session.rs
@@ -1048,6 +1048,7 @@ impl Session {
                     Self::build_model_client_beta_features_header(config.as_ref()),
                     /*item_ids_enabled*/ config.features.enabled(Feature::ItemIds),
                     attestation_provider,
+                    config.model_provider_id.clone(),
                 )
                 .with_prompt_cache_key_override(
                     crate::guardian::prompt_cache_key_override_for_review_session(
diff --git a/codex-rs/core/src/session/tests.rs b/codex-rs/core/src/session/tests.rs
index bca6be6a7..f58835c5d 100644
--- a/codex-rs/core/src/session/tests.rs
+++ b/codex-rs/core/src/session/tests.rs
@@ -463,6 +463,7 @@ fn test_model_client_session() -> crate::client::ModelClientSession {
         /*beta_features_header*/ None,
         /*item_ids_enabled*/ false,
         /*attestation_provider*/ None,
+        /*model_provider_id*/ String::new(),
     )
     .new_session()
 }
@@ -5103,6 +5104,7 @@ pub(crate) async fn make_session_and_context() -> (Session, TurnContext) {
             Session::build_model_client_beta_features_header(config.as_ref()),
             /*item_ids_enabled*/ config.features.enabled(Feature::ItemIds),
             /*attestation_provider*/ None,
+            config.model_provider_id.clone(),
         ),
         code_mode_service: crate::tools::code_mode::CodeModeService::new(),
         tool_search_handler_cache: Default::default(),
@@ -7162,6 +7164,7 @@ where
             Session::build_model_client_beta_features_header(config.as_ref()),
             /*item_ids_enabled*/ config.features.enabled(Feature::ItemIds),
             /*attestation_provider*/ None,
+            config.model_provider_id.clone(),
         ),
         code_mode_service: crate::tools::code_mode::CodeModeService::new(),
         tool_search_handler_cache: Default::default(),
diff --git a/codex-rs/core/src/tools/spec_plan.rs b/codex-rs/core/src/tools/spec_plan.rs
index af7acf17a..3655dae12 100644
--- a/codex-rs/core/src/tools/spec_plan.rs
+++ b/codex-rs/core/src/tools/spec_plan.rs
@@ -304,7 +304,7 @@ fn hosted_model_tool_specs(context: &CoreToolPlanContext<'_>) -> Vec<ToolSpec> {
     // `Some(Cached/Live/Disabled)` are the options for mode when standalone search is unavailable
     // and the provider supports hosted search. `None` prevents emitting a hosted search tool.
     let web_search_mode = (!standalone_web_search_available
-        && turn_context.provider.capabilities().web_search)
+        && provider_capabilities(turn_context).web_search)
         .then_some(turn_context.config.web_search_mode.value());
     let web_search_config = web_search_mode
         .as_ref()
@@ -337,7 +337,25 @@ pub(crate) fn tool_suggest_enabled(turn_context: &TurnContext) -> bool {
 }
 
 fn namespace_tools_enabled(turn_context: &TurnContext) -> bool {
-    turn_context.provider.capabilities().namespace_tools
+    provider_capabilities(turn_context).namespace_tools
+}
+
+/// codez llm-switch:被本地接管的 provider（如 deepseek 走 Chat Completions）在 v1
+/// 仅支持标准 `function` 工具，命名空间 / web_search / image_generation 等托管工具
+/// 无法在目标协议中表达，会触发连接器硬失败（§4.0b）。被 `route()` 接管时一律按
+/// 「无任何托管能力」处理，复用 codex 原生的能力门控从源头不产生这些工具；连接器
+/// 里的硬失败保留作兜底安全网。
+fn provider_capabilities(
+    turn_context: &TurnContext,
+) -> codex_model_provider::ProviderCapabilities {
+    if codez_llm_switch::route(&turn_context.config.model_provider_id).is_some() {
+        return codex_model_provider::ProviderCapabilities {
+            namespace_tools: false,
+            image_generation: false,
+            web_search: false,
+        };
+    }
+    turn_context.provider.capabilities()
 }
 
 fn multi_agent_v2_enabled(turn_context: &TurnContext) -> bool {
@@ -387,7 +405,7 @@ fn image_generation_runtime_enabled(turn_context: &TurnContext) -> bool {
         .auth_manager
         .as_deref()
         .is_some_and(AuthManager::current_auth_uses_codex_backend)
-        && turn_context.provider.capabilities().image_generation
+        && provider_capabilities(turn_context).image_generation
         && turn_context
             .model_info
             .input_modalities
diff --git a/codex-rs/core/tests/responses_headers.rs b/codex-rs/core/tests/responses_headers.rs
index cf9b9a939..5e9e20616 100644
--- a/codex-rs/core/tests/responses_headers.rs
+++ b/codex-rs/core/tests/responses_headers.rs
@@ -129,6 +129,7 @@ async fn responses_stream_includes_subagent_header_on_review() {
         /*beta_features_header*/ None,
         /*item_ids_enabled*/ false,
         /*attestation_provider*/ None,
+        /*model_provider_id*/ String::new(),
     );
     let responses_metadata = test_turn_responses_metadata(&client, thread_id, &session_source);
     let mut client_session = client.new_session();
@@ -261,6 +262,7 @@ async fn responses_stream_includes_subagent_header_on_other() {
         /*beta_features_header*/ None,
         /*item_ids_enabled*/ false,
         /*attestation_provider*/ None,
+        /*model_provider_id*/ String::new(),
     );
     let responses_metadata = test_turn_responses_metadata(&client, thread_id, &session_source);
     let mut client_session = client.new_session();
@@ -379,6 +381,7 @@ async fn responses_respects_model_info_overrides_from_config() {
         /*beta_features_header*/ None,
         /*item_ids_enabled*/ false,
         /*attestation_provider*/ None,
+        /*model_provider_id*/ String::new(),
     );
     let responses_metadata = test_turn_responses_metadata(&client, thread_id, &session_source);
     let mut client_session = client.new_session();
diff --git a/codex-rs/core/tests/suite/client.rs b/codex-rs/core/tests/suite/client.rs
index fe543a875..e59bc1cdb 100644
--- a/codex-rs/core/tests/suite/client.rs
+++ b/codex-rs/core/tests/suite/client.rs
@@ -1163,6 +1163,7 @@ async fn send_provider_auth_request(server: &MockServer, auth: ModelProviderAuth
         /*beta_features_header*/ None,
         /*item_ids_enabled*/ config.features.enabled(Feature::ItemIds),
         /*attestation_provider*/ None,
+        /*model_provider_id*/ String::new(),
     );
     let responses_metadata = test_turn_responses_metadata(&client, thread_id);
     let mut client_session = client.new_session();
@@ -2774,6 +2775,7 @@ async fn azure_responses_request_includes_store_and_reasoning_ids() {
         /*beta_features_header*/ None,
         /*item_ids_enabled*/ false,
         /*attestation_provider*/ None,
+        /*model_provider_id*/ String::new(),
     );
     let responses_metadata = test_turn_responses_metadata(&client, thread_id);
     let mut client_session = client.new_session();
diff --git a/codex-rs/core/tests/suite/client_websockets.rs b/codex-rs/core/tests/suite/client_websockets.rs
index 1ccbd302d..fe03516de 100755
--- a/codex-rs/core/tests/suite/client_websockets.rs
+++ b/codex-rs/core/tests/suite/client_websockets.rs
@@ -2193,6 +2193,7 @@ async fn websocket_harness_with_provider_options(
         /*beta_features_header*/ None,
         /*item_ids_enabled*/ config.features.enabled(Feature::ItemIds),
         /*attestation_provider*/ None,
+        /*model_provider_id*/ String::new(),
     );
 
     WebsocketTestHarness {
diff --git a/codex-rs/memories/write/src/runtime.rs b/codex-rs/memories/write/src/runtime.rs
index 0835e1552..750a88a1d 100644
--- a/codex-rs/memories/write/src/runtime.rs
+++ b/codex-rs/memories/write/src/runtime.rs
@@ -237,6 +237,7 @@ impl MemoryStartupContext {
             /*beta_features_header*/ None,
             config.features.enabled(Feature::ItemIds),
             /*attestation_provider*/ None,
+            config.model_provider_id.clone(),
         );
 
         let mut client_session = model_client.new_session();
