# Rust 编译产物
/target/
**/target/

# build_codex.sh 产物输出目录（编译好的 codex 二进制，不提交）
/release/

# Cargo 锁文件（库项目可取消注释忽略；可执行/工作区一般保留提交）
# Cargo.lock

# 增量编译与构建缓存
**/*.rs.bk

# 后端 panic 时生成的 core dump
*.pdb

# 环境与密钥文件
.env
.env.*
!.env.example

# 编辑器与 IDE
.idea/
.vscode/
*.iml
*.swp
*.swo
*~

# 操作系统生成文件
.DS_Store
Thumbs.db
testkey.toml

# zmod 开发期软链(把 zmod crate 接进 codex-rs workspace 跑测试;dev-only,绝不提交进 codex-rs 子树)
/codex-rs/llm-compress
/codex-rs/llm-switch

/.worktrees

# 工具生成的 AGENTS.md / CLAUDE.md(本地脚手架,不提交;锚定路径以免误伤 codex-rs 子树内同名文件)
/AGENTS.md
/CLAUDE.md
/zmod/llm-switch/AGENTS.md
